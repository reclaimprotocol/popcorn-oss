{
  "name": "attestation",
  "version": "1.0.0",
  "description": "This directory contains standalone scripts to independently verify the cryptographic hardware attestation proofs generated by the Popcorn `attestor` sidecars running inside the AMD SEV-SNP Trusted Execution Environment (TEE).",
  "main": "verify_common.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-forge": "^1.3.3"
  }
}
