worker_processes 1;

events {
    worker_connections 10240;
}

http {
    include       mime.types;
    default_type  application/octet-stream;

    # Session URLs carry bearer JWTs in the third path segment. Never emit the
    # raw request line or query string to pod/Cloud Logging.
    map $uri $redacted_uri {
        ~^/([^/]+)/([^/]+)/([^/]+)(.*)$ /$1/$2/[REDACTED]$4;
        default $uri;
    }
    log_format gateway_redacted '$remote_addr - $remote_user [$time_local] '
        '"$request_method $redacted_uri $server_protocol" $status $body_bytes_sent '
        '"$http_user_agent"';
    access_log /dev/stdout gateway_redacted;
    # Nginx error records append the unredacted request line and cannot use the
    # access-log formatter. Keep only process-level critical failures here;
    # request telemetry belongs in the redacted access log above.
    error_log /dev/stderr crit;

    lua_shared_dict pod_cache 10m;

    resolver RESOLVER_IP valid=5s;

    server {
        listen 80;
        server_name _;
        set $gateway_redis_host GATEWAY_REDIS_HOST;

        # Dynamic Path-Based Routing: /<browser_pod_id>/<session_id>/<token>/...
        location ~ ^/(browser[^/]+)/([^/]+)/([^/]+)(.*) {
            set $pod_id $1;
            set $session_id $2;
            set $token $3;
            set $rest_uri $4;
            set $target_ip '';

            access_by_lua_block {
                local auth = require "auth"
                auth.check(true, ngx.var.token, "restricted", ngx.var.session_id)
                local redis = require "resty.redis"
                local red = redis:new()
                red:set_timeout(1000)

                local session_id = ngx.var.session_id
                local cache = ngx.shared.pod_cache

                local ip = cache:get(session_id)
                if not ip then
                    local ok, err = red:connect("GATEWAY_REDIS_HOST", 6379)
                    if not ok then
                        ngx.log(ngx.ERR, "failed to connect to redis: ", err)
                        return ngx.exit(500)
                    end

                    ip, err = red:get("route:" .. session_id)
                    if not ip or ip == ngx.null then
                        ngx.log(ngx.WARN, "No route found for session: ", session_id)
                        return ngx.exit(404)
                    end

                    cache:set(session_id, ip, 2)
                end

                ngx.var.target_ip = ip
            }

            if ($rest_uri = "") {
                rewrite ^/[^/]+/[^/]+/[^/]+$ / break;
            }
            if ($rest_uri != "") {
                rewrite ^/[^/]+/[^/]+/[^/]+(.*)$ $1 break;
            }

            proxy_pass http://$target_ip;
            
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "Upgrade";
            proxy_set_header Host $http_host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_read_timeout 3600s;
            proxy_send_timeout 3600s;
        }

        # CDP Exposure (Restricted): /cdp/<session_id>/<token>/...
        location ~ ^/cdp/([^/]+)/([^/]+)(.*) {
            set $session_id $1;
            set $token $2;
            set $rest_uri $3;
            set $target_ip '';

            access_by_lua_block {
                local auth = require "auth"
                auth.check(false, ngx.var.token, "restricted", ngx.var.session_id)
                local redis = require "resty.redis"
                local red = redis:new()
                red:set_timeout(1000)

                local session_id = ngx.var.session_id
                local cache = ngx.shared.pod_cache

                local ip = cache:get("cdp:" .. session_id)
                if not ip then
                    local ok, err = red:connect("GATEWAY_REDIS_HOST", 6379)
                    if not ok then
                        ngx.log(ngx.ERR, "failed to connect to redis: ", err)
                        return ngx.exit(500)
                    end

                    ip, err = red:get("route:cdp:" .. session_id)
                    if not ip or ip == ngx.null then
                        ngx.log(ngx.WARN, "No CDP route found for session: ", session_id)
                        return ngx.exit(404)
                    end

                    cache:set("cdp:" .. session_id, ip, 2)
                end

                ngx.var.target_ip = ip
            }

            if ($rest_uri = "") {
                rewrite ^/cdp/[^/]+/[^/]+$ / break;
            }
            if ($rest_uri != "") {
                rewrite ^/cdp/[^/]+/[^/]+(.*)$ $1 break;
            }

            proxy_pass http://$target_ip;

            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "Upgrade";
            proxy_set_header Host $http_host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-Prefix /cdp/$session_id/$token;
            # Disable permessage-deflate compression: iOS/WebKit closes with 1006
            # when a compressed WebSocket frame context goes out of sync through
            # a transparent TCP proxy. Stripping this header forces uncompressed frames.
            proxy_set_header Sec-WebSocket-Extensions "";
            proxy_read_timeout 3600s;
            proxy_send_timeout 3600s;
        }

        # CDP Internal Exposure (Full Access): /cdp-internal/<session_id>/<token>/...
        # Paid agent CDP exposure: an automation-scoped token can reach the full
        # CDP port, but cannot be replayed against internal runtime/API routes.
        location ~ ^/cdp-agent/([^/]+)/([^/]+)(.*) {
            set $session_id $1;
            set $token $2;
            set $rest_uri $3;
            set $target_ip '';

            access_by_lua_block {
                local auth = require "auth"
                auth.check(false, ngx.var.token, "automation", ngx.var.session_id)
                local redis = require "resty.redis"
                local red = redis:new()
                red:set_timeout(1000)

                local session_id = ngx.var.session_id
                local cache = ngx.shared.pod_cache
                local ip = cache:get("cdp-agent:" .. session_id)
                if not ip then
                    local ok, err = red:connect("GATEWAY_REDIS_HOST", 6379)
                    if not ok then
                        ngx.log(ngx.ERR, "failed to connect to redis: ", err)
                        return ngx.exit(500)
                    end

                    ip, err = red:get("route:cdp-internal:" .. session_id)
                    if not ip or ip == ngx.null then
                        ngx.log(ngx.WARN, "No agent CDP route found for session: ", session_id)
                        return ngx.exit(404)
                    end
                    cache:set("cdp-agent:" .. session_id, ip, 2)
                end
                ngx.var.target_ip = ip
            }

            if ($rest_uri = "") {
                rewrite ^/cdp-agent/[^/]+/[^/]+$ / break;
            }
            if ($rest_uri != "") {
                rewrite ^/cdp-agent/[^/]+/[^/]+(.*)$ $1 break;
            }

            proxy_pass http://$target_ip;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "Upgrade";
            proxy_set_header Host $http_host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-Prefix /cdp-agent/$session_id/$token;
            proxy_set_header Sec-WebSocket-Extensions "";
            proxy_read_timeout 3600s;
            proxy_send_timeout 3600s;
        }

        location ~ ^/cdp-internal/([^/]+)/([^/]+)(.*) {
            set $session_id $1;
            set $token $2;
            set $rest_uri $3;
            set $target_ip '';

            access_by_lua_block {
                local auth = require "auth"
                auth.check(false, ngx.var.token, "internal", ngx.var.session_id)
                local redis = require "resty.redis"
                local red = redis:new()
                red:set_timeout(1000)

                local session_id = ngx.var.session_id
                local cache = ngx.shared.pod_cache

                local ip = cache:get("cdp-internal:" .. session_id)
                if not ip then
                    local ok, err = red:connect("GATEWAY_REDIS_HOST", 6379)
                    if not ok then
                        ngx.log(ngx.ERR, "failed to connect to redis: ", err)
                        return ngx.exit(500)
                    end

                    ip, err = red:get("route:cdp-internal:" .. session_id)
                    if not ip or ip == ngx.null then
                        ngx.log(ngx.WARN, "No CDP internal route found for session: ", session_id)
                        return ngx.exit(404)
                    end

                    cache:set("cdp-internal:" .. session_id, ip, 2)
                end

                ngx.var.target_ip = ip
            }

            if ($rest_uri = "") {
                rewrite ^/cdp-internal/[^/]+/[^/]+$ / break;
            }
            if ($rest_uri != "") {
                rewrite ^/cdp-internal/[^/]+/[^/]+(.*)$ $1 break;
            }

            proxy_pass http://$target_ip;

            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "Upgrade";
            proxy_set_header Host $http_host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-Prefix /cdp-internal/$session_id/$token;
            proxy_set_header Sec-WebSocket-Extensions "";
            proxy_read_timeout 3600s;
            proxy_send_timeout 3600s;
        }

        # Kernel API Exposure (requires internal token): /api/<session_id>/<token>/...
        location ~ ^/api/([^/]+)/([^/]+)(.*) {
            set $session_id $1;
            set $token $2;
            set $rest_uri $3;
            set $target_ip '';

            access_by_lua_block {
                local auth = require "auth"
                auth.check(false, ngx.var.token, "internal", ngx.var.session_id)
                local redis = require "resty.redis"
                local red = redis:new()
                red:set_timeout(1000)

                local session_id = ngx.var.session_id
                local cache = ngx.shared.pod_cache

                local ip = cache:get("api:" .. session_id)
                if not ip then
                    local ok, err = red:connect("GATEWAY_REDIS_HOST", 6379)
                    if not ok then
                        ngx.log(ngx.ERR, "failed to connect to redis: ", err)
                        return ngx.exit(500)
                    end

                    ip, err = red:get("route:api:" .. session_id)
                    if not ip or ip == ngx.null then
                        ngx.log(ngx.WARN, "No API route found for session: ", session_id)
                        return ngx.exit(404)
                    end

                    cache:set("api:" .. session_id, ip, 2)
                end

                ngx.var.target_ip = ip
            }

            if ($rest_uri = "") {
                rewrite ^/api/[^/]+/[^/]+$ / break;
            }
            if ($rest_uri != "") {
                rewrite ^/api/[^/]+/[^/]+(.*)$ $1 break;
            }

            proxy_pass http://$target_ip;
            
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "Upgrade";
            proxy_set_header Host $http_host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_read_timeout 3600s;
            proxy_send_timeout 3600s;
        }

        include /usr/local/openresty/nginx/conf/session-routes/*.conf;

        # Canonical LiveView HTTP route. VNC is the only supported streaming
        # mode, so this route is part of the core gateway contract rather than
        # optional session-extension route configuration.
        location ~ ^/liveview/([^/]+)/([^/]+)(.*) {
            set $session_id $1;
            set $token $2;
            set $rest_uri $3;
            set $target_ip '';

            access_by_lua_block {
                local auth = require "auth"
                auth.check(false, ngx.var.token, "restricted", ngx.var.session_id)
                local redis = require "resty.redis"
                local red = redis:new()
                red:set_timeout(1000)

                local session_id = ngx.var.session_id
                local cache = ngx.shared.pod_cache
                local route_keys = {"liveview", "vnc"}
                local ip

                for _, route_key in ipairs(route_keys) do
                    ip = cache:get(route_key .. ":" .. session_id)
                    if ip then
                        break
                    end
                end

                if not ip or ip == ngx.null then
                    local ok, err = red:connect("GATEWAY_REDIS_HOST", 6379)
                    if not ok then
                        ngx.log(ngx.ERR, "failed to connect to redis: ", err)
                        return ngx.exit(500)
                    end

                    for _, route_key in ipairs(route_keys) do
                        ip, err = red:get("route:" .. route_key .. ":" .. session_id)
                        if ip and ip ~= ngx.null then
                            cache:set(route_key .. ":" .. session_id, ip, 2)
                            break
                        end
                    end
                    if not ip or ip == ngx.null then
                        ngx.log(ngx.WARN, "No liveview route found for session: ", session_id)
                        return ngx.exit(404)
                    end
                end

                ngx.var.target_ip = ip
            }

            if ($rest_uri = "") {
                rewrite ^/liveview/[^/]+/[^/]+$ / break;
            }
            if ($rest_uri != "") {
                rewrite ^/liveview/[^/]+/[^/]+(.*)$ $1 break;
            }

            proxy_pass http://$target_ip;
            proxy_http_version 1.1;
            proxy_buffering off;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "Upgrade";
            proxy_set_header Host $http_host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_read_timeout 3600s;
            proxy_send_timeout 3600s;
        }

        # Raw remote desktop/RFB over WebSocket:
        # /liveview-ws/<session_id>/<token> (current)
        # /vnc-ws/<session_id>/<token> (legacy)
        location ~ ^/(liveview-ws|vnc-ws)/([^/]+)/([^/]+)/?$ {
            set $liveview_ws_prefix $1;
            set $session_id $2;
            set $token $3;
            set $target_ip '';

            access_by_lua_block {
                local auth = require "auth"
                auth.check(false, ngx.var.token, "restricted", ngx.var.session_id)
                local redis = require "resty.redis"
                local red = redis:new()
                red:set_timeout(1000)

                local session_id = ngx.var.session_id
                local cache = ngx.shared.pod_cache
                local route_keys
                if ngx.var.liveview_ws_prefix == "liveview-ws" then
                    route_keys = {"liveview", "vnc"}
                else
                    route_keys = {"vnc", "liveview"}
                end

                local ip
                for _, route_key in ipairs(route_keys) do
                    ip = cache:get(route_key .. ":" .. session_id)
                    if ip then
                        break
                    end
                end

                if not ip or ip == ngx.null then
                    local ok, err = red:connect("GATEWAY_REDIS_HOST", 6379)
                    if not ok then
                        ngx.log(ngx.ERR, "failed to connect to redis: ", err)
                        return ngx.exit(500)
                    end

                    for _, route_key in ipairs(route_keys) do
                        ip, err = red:get("route:" .. route_key .. ":" .. session_id)
                        if ip and ip ~= ngx.null then
                            cache:set(route_key .. ":" .. session_id, ip, 2)
                            break
                        end
                    end
                    if not ip or ip == ngx.null then
                        ngx.log(ngx.WARN, "No liveview route found for session: ", session_id, " routes: ", table.concat(route_keys, ","))
                        return ngx.exit(404)
                    end
                end

                ngx.var.target_ip = ip
            }

            proxy_pass http://$target_ip/websockify;

            proxy_http_version 1.1;
            proxy_buffering off;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "Upgrade";
            proxy_set_header Host $http_host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Sec-WebSocket-Extensions "";
            proxy_read_timeout 3600s;
            proxy_send_timeout 3600s;
        }

        # Attestor / Proof API Exposure: /proof/<session_id>
        location ~ ^/proof/([^/]+)(.*) {
            set $session_id $1;
            set $rest_uri $2;
            set $target_ip '';

            access_by_lua_block {
                local redis = require "resty.redis"
                local red = redis:new()
                red:set_timeout(1000)

                local session_id = ngx.var.session_id
                local cache = ngx.shared.pod_cache

                -- For the attestor, we route to the same pod IP as the main browser session,
                -- but we will append the port 8085 in the proxy_pass
                local ip = cache:get(session_id)
                if not ip then
                    local ok, err = red:connect("GATEWAY_REDIS_HOST", 6379)
                    if not ok then
                        ngx.log(ngx.ERR, "failed to connect to redis: ", err)
                        return ngx.exit(500)
                    end

                    ip, err = red:get("route:" .. session_id)
                    if not ip or ip == ngx.null then
                        ngx.log(ngx.WARN, "No route found for session: ", session_id)
                        return ngx.exit(404)
                    end

                    cache:set(session_id, ip, 2)
                end

                -- IP format from pool-manager route cache usually includes the port 8082: e.g., "10.0.1.5:8082"
                -- We must strip the port and replace with 8085.
                -- Using pure Lua string manipulation:
                local ip_only = string.match(ip, "^(.-):")
                if not ip_only then
                    -- Fallback if no port was attached
                    ip_only = ip
                end
                
                ngx.var.target_ip = ip_only .. ":8085"
            }

            if ($rest_uri = "") {
                rewrite ^/proof/[^/]+$ /proof break;
            }
            if ($rest_uri != "") {
                rewrite ^/proof/[^/]+(.*)$ /proof$1 break;
            }

            proxy_pass http://$target_ip;
            
            proxy_http_version 1.1;
            proxy_set_header Host $http_host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_read_timeout 60s;
            proxy_send_timeout 60s;
        }

        # Fallback / Health Check
        location /health {
            return 200 "OK\n";
        }

        # Proxy everything else to Pool Manager (UI + API)
        location / {
            proxy_pass http://GATEWAY_POOL_MANAGER_HOST:80;
            
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "Upgrade";
            proxy_set_header Host $http_host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
}
