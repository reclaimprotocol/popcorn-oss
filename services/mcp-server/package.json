{
  "name": "mcp-server",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "start": "bun run index.ts",
    "dev": "bun run --watch index.ts",
    "test": "STRIPE_WEBHOOK_SECRET=whsec_test bun test",
    "db:migrate": "bun run scripts/migrate.ts"
  },
  "dependencies": {
    "hono": "4.12.34",
    "postgres": "^3.4.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.6"
  }
}
