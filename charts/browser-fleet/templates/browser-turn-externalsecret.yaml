{{- if .Values.externalSecrets.enabled }}
{{- $browserTurnSecret := default "browser-turn-secret" .Values.secrets.browserTurnName }}
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ $browserTurnSecret }}
  namespace: {{ .Release.Namespace }}
  finalizers:
    - externalsecrets.external-secrets.io/externalsecret-cleanup
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: gcpsm
  target:
    name: {{ $browserTurnSecret }}
    creationPolicy: Owner
    deletionPolicy: Retain
  data:
    - secretKey: TURN_KEY_ID
      remoteRef:
        key: browser-turn-key-id
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None
    - secretKey: TURN_API_TOKEN
      remoteRef:
        key: browser-turn-api-token
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None
{{- end }}
