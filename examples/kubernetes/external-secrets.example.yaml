# Provider-neutral External Secrets Operator example.
# Replace SecretStore names and remoteRef keys for your provider.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: gateway-jwt-keys
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: popcorn-secrets
  target:
    name: gateway-jwt-keys
    creationPolicy: Owner
  data:
    - secretKey: private.pem
      remoteRef:
        key: popcorn/gateway/private.pem
    - secretKey: public.pem
      remoteRef:
        key: popcorn/gateway/public.pem
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: pool-manager-env-secrets
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: popcorn-secrets
  target:
    name: pool-manager-env-secrets
    creationPolicy: Owner
  data:
    - secretKey: POPCORN_ADMIN_USER
      remoteRef:
        key: popcorn/pool-manager/admin-user
    - secretKey: POPCORN_ADMIN_PASS
      remoteRef:
        key: popcorn/pool-manager/admin-password
    - secretKey: SERVICE_AUTH_TOKEN
      remoteRef:
        key: popcorn/service-auth-token
    - secretKey: SESSION_AUTH_CLIENTS
      remoteRef:
        key: popcorn/pool-manager/session-auth-clients
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: analytics-service-secret
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: popcorn-secrets
  target:
    name: analytics-service-secret
    creationPolicy: Owner
  data:
    - secretKey: SERVICE_AUTH_TOKEN
      remoteRef:
        key: popcorn/service-auth-token
    - secretKey: ADMIN_TOKEN
      remoteRef:
        key: popcorn/analytics/admin-token
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: analytics-db-secret
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: popcorn-secrets
  target:
    name: analytics-db-secret
    creationPolicy: Owner
  data:
    - secretKey: host
      remoteRef:
        key: popcorn/analytics-db/host
    - secretKey: port
      remoteRef:
        key: popcorn/analytics-db/port
    - secretKey: database
      remoteRef:
        key: popcorn/analytics-db/database
    - secretKey: username
      remoteRef:
        key: popcorn/analytics-db/username
    - secretKey: password
      remoteRef:
        key: popcorn/analytics-db/password
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: browser-turn-secret
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: popcorn-secrets
  target:
    name: browser-turn-secret
    creationPolicy: Owner
  data:
    - secretKey: TURN_KEY_ID
      remoteRef:
        key: popcorn/browser-turn/key-id
    - secretKey: TURN_API_TOKEN
      remoteRef:
        key: popcorn/browser-turn/api-token
    - secretKey: NEKO_ICESERVERS
      remoteRef:
        key: popcorn/browser-turn/ice-servers-json

